#include <iostream>
#include <vector>

using std::vector;


vector<int> getConcatenation(vector<int>& nums){
	for(int i = 0; i < sizeof(nums); i++) {
		size_t ans = sizeof(nums) * 2;
		
	}
	return ans;
}

int main(){
	vector<int> new_vec = {10, 20, 30};
	vector<int> result = getConcatenation(new_vec);

	return 0;
}
